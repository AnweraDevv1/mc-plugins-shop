services:
  - type: web
    name: mc-plugins-shop
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: WEBAPP_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
